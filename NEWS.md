# nlmixr2sse 0.1

* Initial Phase 3 package skeleton, including `runSSEControl()`,
  `sseModel()`, `runSSE()`, and the `nlmixr2SSE` return-class scaffold
  built on top of shared `nlmixr2utils` run-directory and seeding helpers.

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-sse.R
\name{runSSE}
\alias{runSSE}
\title{Run fixed-parameter stochastic simulation and estimation}
\usage{
runSSE(
  fit,
  alternativeModels = NULL,
  samples = 100L,
  seed = NULL,
  control = runSSEControl(),
  outputDir = NULL,
  restart = FALSE,
  fitName = NULL,
  ...
)
}
\arguments{
\item{fit}{Reference fitted \code{nlmixr2} model.}

\item{alternativeModels}{Optional \code{sseModel()} object or list of
\code{sseModel()} objects.}

\item{samples}{Integer number of SSE replicates to run.}

\item{seed}{Optional master seed for the run.}

\item{control}{SSE control object created by \code{\link[=runSSEControl]{runSSEControl()}}.}

\item{outputDir}{Optional output directory. When \code{NULL}, a numbered
\verb{<fitName>_sse_<N>} directory is created or resumed.}

\item{restart}{Logical flag controlling overwrite/resume behavior through the
shared run-directory helper.}

\item{fitName}{Optional fit label used for the run directory and simulation
model metadata. When \code{NULL}, the label is derived from the \code{fit}
expression.}

\item{...}{Reserved for future extensions. Unsupported arguments raise an
error.}
}
\value{
An object of class \code{c("nlmixr2SSE", "list")}.
}
\description{
\code{runSSE()} simulates \code{samples} datasets from a fitted \code{nlmixr2} reference
model, optionally refits the simulation model itself, refits any alternative
hypotheses supplied via \code{\link[=sseModel]{sseModel()}}, writes canonical SSE artifacts, and
returns an \code{nlmixr2SSE} object containing raw results plus tidy parameter and
OFV summaries.
}
\details{
Phase 6 adds resumable run-state tracking, add-models support on completed
SSE directories, and summary recomputation via \code{\link[=recomputeSSE]{recomputeSSE()}}, while
retaining the fixed, raw-results-driven, and covariance-based simulation
modes from Phase 5.
}

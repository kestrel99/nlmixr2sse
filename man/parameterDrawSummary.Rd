% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sse-parameter-diagnostics.R
\name{parameterDrawSummary}
\alias{parameterDrawSummary}
\title{Summarize how well drawn covariance-mode parameters match their targets}
\usage{
parameterDrawSummary(x, ...)
}
\arguments{
\item{x}{An \code{nlmixr2SSE} object produced by a \code{parameterSource = "covariance"} \code{runSSE()} call.}

\item{...}{Unused; reserved for future extension.}
}
\value{
A \code{data.frame} with one row per generating parameter (THETA or
OMEGA entry), with columns:
\describe{
\item{parameter}{The parameter name, as it appears in
\code{x$initialValues$parameter}.}
\item{n}{Finite draw count.}
\item{realized_mean, realized_sd, realized_median, realized_q025,
realized_q975, realized_min, realized_max}{Summary statistics of the
actually-drawn values across replicates.}
\item{target_mean, target_sd}{The Normal/inverse-Wishart target mean
and standard deviation the draw was seeded from, when known.}
\item{dispersion_ratio}{\code{realized_sd / target_sd}; noticeably above 1
for \code{independent_iw}'s non-binding OMEGA elements.}
\item{mean_drift_flag}{\code{TRUE} when \code{realized_mean} deviates from
\code{target_mean} by more than 5\% relatively (a fixed internal
threshold, comfortably below the joint mode's typical drift and
comfortably above ordinary Monte Carlo noise).}
\item{lower, upper}{Recoverable finite THETA bounds from the model's
\code{ini()} block (\code{-Inf}/\code{Inf} when unconstrained); \code{NA} for OMEGA
entries, which have no such domain.}
\item{n_out_of_domain}{Count of replicates whose THETA draw fell
outside \verb{[lower, upper]}. Diagnostic only -- draws are never
truncated or resampled. \code{NA} for parameters with no tracked bounds.}
\item{binding_nu}{The \code{independent_iw} inverse-Wishart degrees of
freedom for this parameter's OMEGA block (shared by every parameter
in the same block). \code{NA} for THETA parameters and for OMEGA blocks
with no usable reported SE.}
\item{n_not_positive_definite}{Count of replicates where this
parameter's OMEGA block failed a positive-definiteness check
(shared by every parameter in the same block). \code{NA} for THETA
parameters.}
}

For \code{covarianceDraw = "joint"} runs with two or more drawn parameters,
the result also carries a \code{"jointDependence"} attribute: a \code{data.frame}
of pairwise empirical correlations between the drawn parameters, computed
from \code{x$initialValues}. This is a descriptive fact about what actually
came out of the draw, not a claim that it reproduces \code{fit$cov} exactly.
}
\description{
For a completed \code{runSSE()} run with \code{parameterSource = "covariance"}, this
reports -- for every generating parameter, across all replicates -- how the
ACTUALLY DRAWN values (\code{x$initialValues}) compare to the INTENDED targets
that seeded the draw (the fitted value and reported standard error from
\code{x$runInfo$parameterSourceInfo$targets}). This is a diagnostic only: it
never modifies, truncates, or resamples the recorded draws.
}
\details{
Two known artefacts, both intentional consequences of the draw machinery
rather than bugs, are what this table is mainly for surfacing:
\itemize{
\item Under \code{covarianceDraw = "joint"}, OMEGA is drawn on a log-Cholesky scale
(unconstrained, so a plain multivariate-Normal draw always back-
transforms to a positive-definite matrix). That is unbiased on the
transformed scale but inflates the RAW-scale mean upward -- watch
\code{realized_mean} versus \code{target_mean} and \code{mean_drift_flag}.
\item Under \code{covarianceDraw = "independent_iw"}, each OMEGA block is drawn from
an inverse-Wishart with a single degrees-of-freedom \code{nu} (\code{binding_nu}),
chosen so that exactly one diagonal element -- the "binding" one --
matches its reported SE exactly. Every other element in the same block
comes out more dispersed than reported -- watch \code{dispersion_ratio}
(\code{realized_sd / target_sd}), which will exceed 1 for non-binding
elements.
}

THETA out-of-bound draws (\code{n_out_of_domain}) and OMEGA positive-
definiteness failures (\code{n_not_positive_definite}) are also counted per
parameter; both draw modes are positive-definite by construction (see
R/sse-omega-draw.R and R/sse-omega-joint.R), so the latter should be \code{0} in
virtually every realistic case, and a nonzero count is itself diagnostic of
an extreme, ill-conditioned fit.
}

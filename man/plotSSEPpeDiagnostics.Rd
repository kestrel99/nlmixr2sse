% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-sse-ppe-diagnostics.R
\name{plotSSEPpeDiagnostics}
\alias{plotSSEPpeDiagnostics}
\title{Diagnose whether the fitted noncentral chi-square describes the observed test statistics}
\usage{
plotSSEPpeDiagnostics(
  x,
  comparisons = NULL,
  models = NULL,
  nonpositive = c("warn", "error", "drop"),
  conf.level = 0.95,
  bootstrapSamples = 1000L,
  bootSeed = NULL,
  ...
)
}
\arguments{
\item{x}{An \code{nlmixr2SSE} object.}

\item{comparisons}{Optional \code{sseComparison()} object or list of them. When
\code{NULL}, uses \code{x$runInfo$comparisons}, then the legacy
simulation-vs-alternative comparisons (with a warning that \code{df} was
inferred rather than declared; see \code{.resolveComparisons()}). With more
than one comparison, the plot facets by comparison and the
\code{"ppeDiagnostics"} attribute has one row per comparison.}

\item{models}{Optional subset of alternative-model labels, used only when
\code{comparisons} is \code{NULL}. Mutually exclusive with \code{comparisons}.}

\item{nonpositive}{What to do when a comparison has non-positive test
statistics excluded from the fit: \code{"warn"} (the default), \code{"error"}, or
\code{"drop"} (silent). See \code{.ppeApplyNonpositivePolicy()}.}

\item{conf.level}{Confidence level for the pointwise ECDF envelope.}

\item{bootstrapSamples}{Number of parametric-bootstrap replicates per
comparison, used both for the p-value's null distribution and for the
ECDF envelope.}

\item{bootSeed}{Optional integer seed for the bootstrap. \code{NULL} (the
default) derives a seed deterministically from the run and each
comparison's label (\code{.ppeDefaultSeed()}), so repeated calls on the same
run reproduce the same diagnostic without the caller managing seeds.}

\item{...}{Reserved for future extensions.}
}
\value{
A \code{ggplot} object (a \code{patchwork} combination of an ECDF panel and
a QQ panel), with the numeric diagnostics attached as a
\code{"ppeDiagnostics"} attribute (one row per comparison, with \code{comparison},
\code{parameter} (\code{"ncp"} or \code{"df"}, whichever was diagnosed), \code{estimate}
(the fitted value of that parameter -- the value the discrepancy and
p-value are actually about), \code{cvm}, \code{p_value}, \code{n}, \code{n_nonpositive},
\code{df} (the comparison's declared/nominal df, kept comparable across
power and type1 rows; for a Type-I comparison this can differ from
\code{estimate}), and \code{df_source}) so results are auditable without reading
pixels.
}
\description{
\code{ppeSummary()}/\code{plotSSEPpePower()} treat the retained likelihood-ratio test
statistics as draws from a noncentral chi-square (Ueckert, Karlsson &
Hooker 2016; the method PsN implements as \code{distribution_mle}) and fit it
by maximum likelihood. Nothing in that pipeline checks whether the
noncentral chi-square actually describes the data -- this function does:
it computes a Cramer-von Mises discrepancy between the empirical CDF of
the retained statistics and the fitted chi-square CDF, with a
parametric-bootstrap p-value (draw synthetic data from the FITTED model,
refit, recompute the discrepancy, and see how often a correctly-specified
world produces one at least this large -- see \code{.ppeDiagnosticPValue()}),
and renders an ECDF-vs-fitted-CDF panel with a pointwise bootstrap
envelope alongside a QQ panel.
}
\details{
Both \code{power} and \code{type1} comparisons can be diagnosed: a power
comparison's noncentrality is the value diagnosed (df held at the
comparison's known value); a Type-I comparison's df is the value
diagnosed (ncp held at 0) -- exactly the \code{target} split
\code{.ppeParametricBootstrap()} and \code{.ppeFit()} already use.

This is diagnostic evidence about how well the noncentral chi-square
describes the observed data, not a pass/fail certification. A small
\code{p_value} says the fitted distribution is a poor description of the real
data; it does not by itself invalidate \code{ppeSummary()}'s point estimate,
and a large \code{p_value} does not prove the assumption exactly right, only
not detectably wrong at this sample size. Read it the way \code{.ppeFit()}'s
boundary-solution warning is read: a flag to weigh alongside the estimate,
not a gate on using it.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recompute-sse.R
\name{recomputeSSE}
\alias{recomputeSSE}
\title{Recompute SSE summaries from an existing run directory}
\usage{
recomputeSSE(
  runDir,
  outFilter = NULL,
  refOfv = NULL,
  samples = NULL,
  overwrite = FALSE,
  comparisons = NULL
)
}
\arguments{
\item{runDir}{Completed SSE run directory.}

\item{outFilter}{Optional raw-results filter applied before summary
calculations. This accepts the same forms as
\code{\link[nlmixr2utils:setupRawResultsFilter]{nlmixr2utils::setupRawResultsFilter()}}.}

\item{refOfv}{Optional OFV override used when recomputing the OFV summary.}

\item{samples}{Optional integer number of replicates to include from the
start of the saved run.}

\item{overwrite}{Logical. When \code{FALSE}, writes
\verb{sse_results_recompute<k>.csv} and \verb{sse_summary_recompute<k>.rds}.
When \code{TRUE}, replaces \code{sse_results.csv} and \code{sse_summary.rds}.}

\item{comparisons}{Optional \code{sseComparison()} object or list of
\code{sseComparison()} objects to report instead of the run's recorded
comparisons. A comparison is a reporting definition, not an input to
simulation or estimation, so supplying it here does not refit or
resimulate anything -- it only replaces \code{runInfo$comparisons} on the
returned object.}
}
\value{
An object of class \code{c("nlmixr2SSE", "list")}.
}
\description{
\code{recomputeSSE()} rebuilds the parameter and OFV summary artifacts from an
existing completed SSE run without rerunning any simulations or fits. This
is useful when you want to apply a different \code{outFilter}, use a different
\code{refOfv}, or recompute the summaries on a subset of the completed
replicates.
}

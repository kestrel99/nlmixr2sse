% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sse-model.R
\name{sseModel}
\alias{sseModel}
\title{Define an SSE alternative model}
\usage{
sseModel(model, est = NULL, control = NULL, label = NULL, data = NULL)
}
\arguments{
\item{model}{Required model specification. This may be either a fitted
\code{nlmixr2FitCore} object or an unfitted model/UI object.}

\item{est}{Optional estimation method. Required when \code{model} is not a fitted
object; otherwise defaults to the method recorded on the fit.}

\item{control}{Optional estimation control object. Required when \code{model} is
not a fitted object; otherwise defaults to the control recorded on the fit.}

\item{label}{Optional short label used in SSE outputs. When \code{NULL},
\code{\link[=runSSE]{runSSE()}} auto-generates \code{alt1}, \code{alt2}, and so on.}

\item{data}{Optional data override. Reserved for future SSE phases.}
}
\value{
An object of class \code{nlmixr2SSEModel}.
}
\description{
\code{sseModel()} stores the model metadata that \code{\link[=runSSE]{runSSE()}} will use when it
refits alternative hypotheses against each simulated dataset. In Phase 3,
the function resolves labels and estimation metadata so later phases can add
the simulation core without changing the user-facing interface.
}

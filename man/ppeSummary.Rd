% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sse-ppe.R
\name{ppeSummary}
\alias{ppeSummary}
\title{Model-based parametric power estimation summary}
\usage{
ppeSummary(
  x,
  comparisons = NULL,
  models = NULL,
  conf.level = 0.95,
  nonpositive = c("warn", "error", "drop"),
  bootstrapSamples = 1000L,
  bootSeed = NULL
)
}
\arguments{
\item{x}{An \code{nlmixr2SSE} object.}

\item{comparisons}{Optional \code{sseComparison()} object or list of them. When
\code{NULL}, uses \code{x$runInfo$comparisons}, then the legacy
simulation-vs-alternative comparisons (with a warning that \code{df} was
inferred rather than declared; see \code{.resolveComparisons()}).}

\item{models}{Optional subset of alternative-model labels, used only when
\code{comparisons} is \code{NULL}. Mutually exclusive with \code{comparisons}.}

\item{conf.level}{Confidence level for the bootstrap interval.}

\item{nonpositive}{What to do when a comparison has non-positive test
statistics excluded from the fit: \code{"warn"} (the default), \code{"error"}, or
\code{"drop"} (silent). See \code{.ppeApplyNonpositivePolicy()}.}

\item{bootstrapSamples}{Number of parametric-bootstrap replicates per
comparison. \code{0} skips the bootstrap and returns \code{NA} intervals while
still reporting the point estimate and all counts -- the cheap option
when the interval is not needed.}

\item{bootSeed}{Optional integer seed for the bootstrap. \code{NULL} (the
default) derives a seed deterministically from the run and the
comparison's label (\code{.ppeDefaultSeed()}), so repeated calls on the same
run reproduce the same interval without the caller managing seeds.}
}
\value{
A \code{data.frame} with one row per comparison.
}
\description{
One row per comparison, combining the \code{distribution_mle} point estimate
(\code{.ppeChiSquareMle()}) with its parametric-bootstrap interval
(\code{.ppeParametricBootstrap()}, always \code{interval_type = "model_based"}: the
bootstrap draws from the FITTED noncentral chi-square, so the interval
covers estimator variability under the fitted model only, never model
misspecification, and is never "empirical") and provenance (\code{df_source},
\code{boundary}, retained/discarded counts). This is the summary-table
counterpart to \code{plotSSEPpePower()}'s curve/point-range rendering: same
estimator, same bootstrap, tabular rather than plotted.
}

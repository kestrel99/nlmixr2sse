% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sse-comparison-summary.R
\name{comparisonSummary}
\alias{comparisonSummary}
\title{Empirical operating characteristics for explicit comparisons}
\usage{
comparisonSummary(
  x,
  comparisons = NULL,
  models = NULL,
  conf.level = 0.95,
  minPairedFraction = 0.5
)
}
\arguments{
\item{x}{An \code{nlmixr2SSE} object.}

\item{comparisons}{One or more \code{\link[=sseComparison]{sseComparison()}} objects, or \code{NULL} to use
\code{x$runInfo$comparisons}.}

\item{models}{Optional subset of alternative-model labels, used only when
\code{comparisons} is \code{NULL} to build the legacy simulation-vs-alternative
comparisons. Mutually exclusive with \code{comparisons}.}

\item{conf.level}{Confidence level for the binomial interval.}

\item{minPairedFraction}{Minimum fraction of attempted replicates that must
be paired evaluable before a low-yield warning is issued.}
}
\value{
A \code{data.frame} with one row per comparison.
}
\description{
Computes paired-evaluable empirical power/Type-I summaries for one or more
\code{\link[=sseComparison]{sseComparison()}}s: for each comparison, the test statistic
\code{T = OFV_reduced - OFV_full} is computed per replicate, and a replicate
only counts toward the denominator if both models produced a finite,
accepted OFV for it. Uncertainty is an exact Clopper-Pearson binomial
interval on that empirical proportion -- purely a function of the observed
counts, with no distributional assumption about \code{T}. This is distinct from
the model-based (parametric bootstrap) uncertainty computed elsewhere,
hence \code{interval_type} and the \code{mcse_probability} naming.
}

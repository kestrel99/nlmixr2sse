% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-sse-parameter-draws.R
\name{plotSSEParameterDraws}
\alias{plotSSEParameterDraws}
\title{Plot realized covariance-mode parameter draws against their targets}
\usage{
plotSSEParameterDraws(x, ...)
}
\arguments{
\item{x}{An \code{nlmixr2SSE} object produced by a \code{parameterSource = "covariance"} \code{runSSE()} call.}

\item{...}{Reserved for future extension.}
}
\value{
A \code{ggplot} object, one facet panel per drawn parameter
(\code{ggplot2::facet_wrap(~parameter, scales = "free")}), with
\code{parameterDrawSummary(x)}'s return value attached as a
\code{"parameterDrawSummary"} attribute -- so the numeric diagnostic (target
means/SDs, \code{mean_drift_flag}, \code{dispersion_ratio}, and the rest of that
table) is always available without reading pixels off the plot.
}
\description{
For a completed \code{runSSE()} run with \code{parameterSource = "covariance"}, this
plots -- for every drawn generating parameter (THETA or OMEGA) -- a
histogram of the ACTUALLY DRAWN replicate values (\code{x$initialValues}), with
a vertical reference line at that parameter's \code{target_mean} (the fitted
value the draw was seeded from, from \code{parameterDrawSummary(x)}). It is the
visual counterpart to \code{parameterDrawSummary()}, which reports the same
comparison numerically -- see that function's documentation for what the
two known draw-mode artefacts (\code{covarianceDraw = "joint"} raw-scale mean
drift and \code{covarianceDraw = "independent_iw"} non-binding-element
over-dispersion) look like in the histogram: a joint-mode histogram
visibly shifted right of its reference line, or an independent_iw-mode
histogram visibly wider than its target SE would suggest.
}
\details{
All validation is delegated to \code{parameterDrawSummary(x)}, called first: it
asserts \code{x} is an \code{nlmixr2SSE} object and aborts if
\code{x$runInfo$parameterSource} is not \code{"covariance"}. This function does not
duplicate that check or its error message.
}

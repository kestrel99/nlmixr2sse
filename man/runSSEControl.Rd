% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sse-control.R
\name{runSSEControl}
\alias{runSSEControl}
\title{Configure an \code{nlmixr2sse} run}
\usage{
runSSEControl(
  estimateSimulation = TRUE,
  refOfv = NULL,
  parameterSource = c("fixed", "rawres", "covariance"),
  covarianceDraw = c("independent_iw", "joint"),
  omegaRseWarn = 0.5,
  rawresInput = NULL,
  offsetRawres = 1L,
  inFilter = NULL,
  outFilter = NULL,
  referenceInitials = c("model", "simulation"),
  alternativeInitials = c("model", "simulation"),
  randomEstimationInits = lifecycle::deprecated(),
  updateFix = FALSE,
  appendColumns = NULL,
  simulationPostProcess = NULL,
  initialEtas = FALSE,
  workers = NULL,
  rxThreads = "auto",
  addModels = FALSE,
  saveFits = TRUE,
  saveDatasets = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{estimateSimulation}{Logical. Should the simulation model also be
re-estimated on each simulated dataset?}

\item{refOfv}{Optional reference OFV override.}

\item{parameterSource}{Source of simulation parameters: fixed fit values,
canonical raw-results input, or multivariate-normal covariance draws.}

\item{covarianceDraw}{How \code{parameterSource = "covariance"} draws parameters.
\code{"independent_iw"} (the default) draws THETA multivariate-Normal and OMEGA
from a mean-centred inverse-Wishart per OMEGA block, independently of each
other. \code{"joint"} instead draws THETA and OMEGA together from \code{fit$cov} on a
log-Cholesky-transformed scale, incorporating the estimated THETA/OMEGA
covariance through a first-order delta approximation. Both give
positive-definite OMEGA draws.

\code{"joint"} uses information \code{"independent_iw"} discards, but its non-linear
back-transform inflates OMEGA means by roughly \code{exp(SE^2 / (2 * Omega^2))}
— about 2\% at 20\% relative standard error, but 65\% at 100\% — so it is
opt-in rather than the default. Neither mode implements NONMEM's
\verb{$PRIOR NWPRI}, and neither claims PsN parity.}

\item{omegaRseWarn}{Relative-standard-error threshold above which a drawn
OMEGA variance triggers a weak-identification warning naming the block and
eta. Defaults to \code{0.5}.}

\item{rawresInput}{Optional raw-results input path or object for
\code{parameterSource = "rawres"}. This is required when
\code{parameterSource = "rawres"}.}

\item{offsetRawres}{Integer sample offset for raw-results input.}

\item{inFilter, outFilter}{Optional raw-results filters. These accept the same
forms as \code{\link[nlmixr2utils:setupRawResultsFilter]{nlmixr2utils::setupRawResultsFilter()}}.}

\item{referenceInitials, alternativeInitials}{Estimation starting-value
policy for, respectively, the simulation (reference) model refit and the
alternative-model refits. \code{"model"} (the default for both) starts each
fit from the model's own initial estimates, as before. \code{"simulation"}
starts a fit from that replicate's generating parameter vector instead --
a numerical intervention only: it changes optimiser starting values, not
the simulated data. It only has an effect when
\code{parameterSource = "rawres"} -- the same restriction the deprecated
\code{randomEstimationInits} enforced as a hard error. Under \code{"fixed"} or
\code{"covariance"} it is instead a silent no-op, matching this package's
existing "simulation" starts implementation, which is wired up only for
\code{"rawres"}. Splitting the setting by role lets a reference-only or
alternative-only sensitivity study be run without touching the other
role's starts.}

\item{randomEstimationInits}{Deprecated. Use \code{referenceInitials} and
\code{alternativeInitials} instead. \code{TRUE} maps to
\code{referenceInitials = "simulation"} and \code{alternativeInitials = "simulation"}; \code{FALSE} maps both to \code{"model"}.}

\item{updateFix}{Logical. When \code{TRUE}, raw-results-driven SSE updates fixed
model values from the selected raw-results row before refitting.}

\item{appendColumns}{Optional character vector of simulated-data columns to
append to SSE outputs.}

\item{simulationPostProcess}{Optional function applied to each simulated
estimation data set after simulated observations have been merged back
onto the original design data. The function must return a data frame. It
may declare any subset of the named arguments \code{data}, \code{sample},
\code{paramSet}, \code{solved}, \code{referenceData}, and \code{outputDir}.}

\item{initialEtas}{Reserved for later work. Must remain \code{FALSE} in the
current release.}

\item{workers}{Worker setting passed through to shared worker helpers.}

\item{rxThreads}{Number of rxode2 threads each worker may use. \code{"auto"}
(the default) divides the machine's cores among the workers. A positive
integer sets the count explicitly; \code{NULL} defers to rxode2's own default.
Note that rxode2's thread count changes simulated values even under a
fixed seed, so this setting affects reproducibility, not only speed.}

\item{addModels}{Logical. Extend a completed run by fitting only new
alternative models on the saved simulated datasets.}

\item{saveFits, saveDatasets}{Logical retention flags used by restart and
add-models workflows.}

\item{overwrite}{Logical. Reserved output-overwrite flag for later phases.}
}
\value{
An object of class \code{nlmixr2SSEControl}.
}
\description{
\code{runSSEControl()} constructs and validates the control object used by
\code{\link[=runSSE]{runSSE()}}. Phase 7 keeps the Phase 6 uncertainty-mode controls, resumable
add-models workflow, raw-results filtering, and estimation-initialization
switches, and now also exposes simulation post-processing hooks while still
holding \code{initialEtas} back pending stability work.
}
